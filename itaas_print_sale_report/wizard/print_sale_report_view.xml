<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="view_print_sale_report_form" model="ir.ui.view">
        <field name="name">Print Sale Report</field>
        <field name="model">print.sale.report</field>
        <field name="arch" type="xml">
            <form string="Print Sales Order">
                <group>
                    <group>
                        <field name="report_type" required="1" />
                        <field name="show_product_code_on_sale"/>
                    </group>
                </group>
                <footer>
                    <button name="print_report_pdf" string="Print" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_view_print_sale_report" model="ir.actions.act_window">
        <field name="name">Print Report</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">print.sale.report</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="groups_id" eval="[(4,ref('sales_team.group_sale_salesman'))]"/>
        <!-- TODO: check if we need this -->
        <field name="binding_model_id" ref="sale.model_sale_order" />
        <field name="view_id" ref="view_print_sale_report_form"/>
    </record>

</odoo>
